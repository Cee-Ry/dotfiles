ssh-keygen -t ed25519 -C "eduaveceasar@gmail.com"

eval "$(ssh-agent -s)"

ssh-add ~/.ssh/id_ed25519

cat ~/.ssh/id_ed25519.pub
(copy the whole thing)

(past in the link)
https://github.com/settings/keys

ssh -T git@github.com
(check)

(lastly)
echo 'eval "$(ssh-agent -s)" > /dev/null' >> ~/.bashrc
